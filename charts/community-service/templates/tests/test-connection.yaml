apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "community-service.fullname" . }}-test-connection"
  labels:
    {{- include "community-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "community-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
